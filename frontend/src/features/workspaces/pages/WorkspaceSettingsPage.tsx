import { PageHeader, PageLayout } from "@/components/layout";

export function WorkspaceSettingsPage() {
  return (
    <PageLayout>
      <PageHeader
        title="Workspace Settings"
        description="Workspace settings will be available in a future roadmap phase."
      />
    </PageLayout>
  );
}
